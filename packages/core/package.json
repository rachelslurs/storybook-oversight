{
  "name": "oversight-core",
  "version": "0.0.0",
  "description": "Pure, Storybook-free diagnostic engine for the components manifest. Bundled into the addon and the CLI; never published.",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./package.json": "./package.json"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  }
}
