{
  "name": "oversight-mcp-projection",
  "version": "0.0.0",
  "description": "Records what Storybook's MCP server renders from a components manifest, so Oversight's rules can be judged against the text an agent receives rather than the manifest it lints. Never published.",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "@storybook/mcp": "0.8.0"
  }
}
