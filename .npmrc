# Flat node_modules so every workspace package resolves the toolchain declared at
# the workspace root (tsup, typescript, react, the storybook packages, testing
# libs). Published packages ship only their built dist plus declared peer deps, so
# the flat dev layout does not leak into what consumers install.
node-linker=hoisted
