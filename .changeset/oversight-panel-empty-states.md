---
'storybook-addon-oversight': patch
---

The panel's nothing-to-show states now use Storybook's own `EmptyTabContent`, so an empty Oversight tab reads like every other empty tab in the drawer. The Docs block keeps its inline message, which suits the page it sits under.
