---
'storybook-addon-oversight': patch
---

The Docs block now opens with a section heading styled like Storybook's own, rather than a caption bar inside the box, so it reads as part of the page. The heading carries `id="oversight"`, making `#oversight` a stable deep link into any component's Docs page.
