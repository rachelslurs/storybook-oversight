---
'storybook-addon-oversight': patch
---

The undocumented props read as a table rather than a bulleted list, in the treatment the Controls panel gives its own prop table: a muted heading over each column, a rule between rows, and no cell borders or striping on either surface.
