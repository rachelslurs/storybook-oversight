---
'storybook-addon-oversight': patch
---

Spans in the report take the size of what they sit in on a Docs page. The page sizes every span it does not recognize at 16px, so the clean state's emoji rendered larger than the line it sits on.
