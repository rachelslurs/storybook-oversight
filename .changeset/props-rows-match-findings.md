---
'storybook-addon-oversight': patch
---

The undocumented props read as rows rather than a bulleted list, in the same shape the findings above them use: the prop name, then a badge for what is missing. Both sections of a report now scan the same way.
