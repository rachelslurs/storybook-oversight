---
'storybook-addon-oversight': patch
---

A component with no description reads _None_, in italics, and stops. It named the file the component sits in, which the finding below it already answers with what to do. A clean component reads `no findings` 👏 and stops too.
