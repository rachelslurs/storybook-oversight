---
'storybook-addon-oversight': patch
---

The props table stands without a heading over it. Its first column is headed `Prop`, which said the same thing twice. The heading stays on the two cases that have no table to say it: a component with no props extracted, and a manifest whose prop payload was not recognized.
