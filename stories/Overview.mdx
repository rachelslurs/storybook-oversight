import { Meta } from '@storybook/addon-docs/blocks';
import { styled } from 'storybook/theming';
import icon from '../assets/oversight-icon-128.png';
import panelShot from './assets/panel.png';
import blockShot from './assets/block.png';
import driftShot from './assets/manifest-drift.png';
import findingsShot from './assets/findings.png';

export const Figure = styled.figure(({ theme }) => ({
  margin: '1.5rem 0',
  padding: 16,
  border: `1px solid ${theme.appBorderColor}`,
  borderRadius: 8,
  background: theme.background.app,
}));

export const Shot = styled.img(({ theme }) => ({
  width: '100%',
  display: 'block',
  borderRadius: 4,
  // the screenshots carry their own light background, so without an edge they
  // bleed into a light figure and float on a dark one
  border: `1px solid ${theme.appBorderColor}`,
}));

export const Caption = styled.figcaption(({ theme }) => ({
  padding: 0,
  textAlign: 'center',
  // every value is a theme token, not a hex literal: hardcoding the light
  // palette put this at 1.43:1 on a dark Docs page, because the text follows
  // the theme and a fixed tint does not
  fontWeight: theme.typography.weight.bold,
  color: theme.color.defaultText,
  // MDX wraps the caption text in a paragraph, which a styled component can
  // reach and an inline style could not
  '& p': { margin: 0 },
}));

export const Pointer = styled.span(({ theme }) => ({
  display: 'block',
  // shrink to the text so the auto margins center it under the caption
  width: 'fit-content',
  margin: '0 auto',
  fontWeight: theme.typography.weight.regular,
  // the caption text is a paragraph and picks up the docs stylesheet's size; a
  // bare span does not, so without this the pointer outsizes what it annotates
  fontSize: theme.typography.size.s2,
  lineHeight: '24px',
  '& a': { color: theme.color.secondary },
}));

<Meta title="Overview" />

<div style={{ textAlign: 'center', margin: '2.5rem 0 3rem' }}>
  <img src={icon} width="80" height="80" alt="Oversight" style={{ borderRadius: 18, margin: '0 auto' }} />
  <h1 style={{ margin: '1rem 0 0.25rem' }}>storybook-addon-oversight</h1>
  <p style={{ fontSize: 16, color: '#64748b', margin: '0.75rem auto 0', maxWidth: 440, lineHeight: 1.5 }}>
    See what your agent can and can't see: it diagnoses silent failures in your Storybook MCP manifest.
  </p>
</div>

This Storybook dogfoods the addon: `@storybook/addon-mcp` serves the <a href="./manifests/components.html" target="_blank" rel="noreferrer">components manifest</a>, and Oversight lints it.

Open any component below and watch the panel and the block react to what it actually documents.

## Two places to look

### 1 · The panel

Pick a component in the sidebar, open its story, then click the Oversight tab in the addons panel.

<Figure>
  <Shot src={panelShot} alt="The Oversight panel on the Card story, flagging an undocumented required prop" />
  <Caption>
    An Oversight tab in the addons panel, on every component's Story view, with a count badge of how many issues it
    found
    <Pointer>Pictured above: [Card](?path=/story/data-display-card--default)</Pointer>
  </Caption>
</Figure>

### 2 · The Docs block

Scroll to the end of any component's Docs to find it. The block is optional. Follow the [global or per-page setup instructions](https://github.com/rachelslurs/storybook-oversight/blob/main/packages/storybook-addon-oversight/README.md#optional-enable-the-docs-page-block) to add it to your Storybook:

<Figure>
  <Shot src={blockShot} alt="The Oversight coverage block at the bottom of the Card Docs page" />
  <Caption>
    The same report, rendered inline at the bottom of every Docs page
    <Pointer>Pictured above: [Card](?path=/docs/data-display-card--docs#oversight)</Pointer>
  </Caption>
</Figure>

## The examples

Each component is built to demonstrate one rule. Four trip a finding, two pass.

<Figure style={{ maxWidth: 600, marginInline: 'auto' }}>
  <Shot
    src={findingsShot}
    alt="The Findings section showing an error and a warning: a severity chip, the rule name, and a message for each"
  />
  <Caption>
    A failing rule renders as a severity chip, the rule name, and a one-line message
    <Pointer>Pictured above: [Card](?path=/story/data-display-card--default)</Pointer>
  </Caption>
</Figure>

Open each component and watch the Oversight tab (Story view) or scroll to the block (Docs view):

- [Button](?path=/docs/actions-button--docs): fully documented, and its description ends with a valid `?path=` redirect to Card → clean, and the link renders clickable in the panel.
- [Card](?path=/docs/data-display-card--docs): an undocumented required prop → `required-prop-undocumented` (error) plus `prop-descriptions-missing` (warning). _(pictured above)_
- [Badge](?path=/docs/data-display-badge--docs): no component description → `component-description-missing` (warning).
- [Tile](?path=/docs/data-display-tile--docs): a `?path=` link pointing at a component that isn't in the manifest → `docs-link-dangling` (error).
- [Banner](?path=/docs/feedback-banner--docs): a `@deprecated` tag → `deprecated-tag` (info).
- [Panel](?path=/docs/layout-panel--docs): an undocumented `slot` prop that would trip `prop-descriptions-missing`, but `@oversightIgnore` exempts this component from that rule → clean.

## Why these are rules

`@storybook/addon-mcp` already lets you _view_ the raw manifest. Three checks in this demo need judgment that reading it can't give you:

- `extractor-drift` is a comparison. The manifest looks fine on its own; it's only wrong relative to the extractor you expected.
- `docs-link-dangling` takes every id in the manifest to know a `?path=` redirect points at nothing. One component's entry can't tell (the panel strikes the dead link through where it appears in the description).
- `required-prop-undocumented` vs `prop-descriptions-missing`: every blank prop reads the same in a raw view. Oversight calls the required one an `error`, because that's the gap an agent is most likely to guess through.

A fourth, `prop-shape-unrecognized`, needs a manifest whose prop payload has moved, which this demo can't stage. [Why these are lint rules](https://github.com/rachelslurs/storybook-oversight/blob/main/docs/why-lint-rules.md) makes the whole argument.

## The manifest-level rule

`extractor-drift` isn't tied to one component. It fires when the manifest was built with a different docgen extractor than the one you state as `expectedExtractor` (addon config, or `--expected-extractor` in the CLI); with no expectation stated, the rule does not run. The shallow `react-docgen` parser silently drops complex prop types, so descriptions come back empty across the board. Because it's a property of the whole manifest, the panel reports it separately:

<Figure>
  <Shot
    src={driftShot}
    alt="The Oversight panel's Manifest section flagging extractor-drift, above the component's findings"
  />
  <Caption>
    `extractor-drift` in its own Manifest section, above the per-component findings
    <Pointer>Pictured above: [Card](?path=/story/data-display-card--default)</Pointer>
  </Caption>
</Figure>

This demo ships on `react-docgen-typescript`, so drift can't fire live. The still above is a build with the shallow extractor; in a project that states its expectation, the warning appears the moment the manifest drifts.

## Not shown here

`docgen-missing` and `story-extraction-error` fire only on extraction failures, which can't be staged with well-formed components. They're covered by the addon's unit tests instead.

## Where to read more

- [Rules](https://github.com/rachelslurs/storybook-oversight/blob/main/docs/rules.md): what each one fires on, its default severity, and how to change it
- [Troubleshooting](https://github.com/rachelslurs/storybook-oversight/blob/main/docs/troubleshooting.md): a fix for every rule, including the two above that can't be staged here
- [Authoring MCP-legible docs](https://github.com/rachelslurs/storybook-oversight/blob/main/docs/authoring.md): writing component docs an agent can act on

Run it yourself with [`storybook-addon-oversight`](https://github.com/rachelslurs/storybook-oversight/blob/main/packages/storybook-addon-oversight/README.md) for the panel, or [`oversight-lint`](https://github.com/rachelslurs/storybook-oversight/blob/main/packages/cli/README.md) to fail CI when a component's docs regress.
